<% if !@user.display_name.blank? %>
  <% provide(:title, "#{@user.display_name}") %>
<% else %>
  <% provide(:title, "#{@user.username}") %>
<% end %>

<div style="background-color: <%= @user.topbg_colour %>" class="ui inverted vertical segment">
  <div class="ui container">
    <h2 style="color: <%= @user.name_colour %> !important" class="ui orange center aligned header">
      <% if !@user.display_name.blank? %>
        <%= @user.display_name %> 
      <% else %>
        <%= @user.username %> 
      <% end %> 
      <div class="sub header"><%= link_to nice_url(request.original_url), request.original_url, {style: "color: #{@user.singlelink_colour}"} %></div>
    </h2>
    <%= image_tag(@user.avatar.thumb.url, class: "ui centered tiny circular image") %>
    <h3 id="break" style="color: <%= @user.bio_colour %> !important" class="ui orange center aligned header">
      <%= @user.bio %>
    </h3>
  </div>
</div>

<div id="main" class="ui container">
  <% if !@links.empty? %>
    <div id="custom_cards" class="ui four link cards">
      <% @links.each do |link| %>
        <% if link.disable == false %>
          <%= link_to link.url, class: "card", :target => "_blank" do %>
            <% if link.default_image.present? && link.image.blank? %>
              <div id="<%= link.default_image %>" class="image">
                <%= image_tag link.default_image + ".png" %>
              </div>
            <% else %>
              <div class="image">
                <%= image_tag link.image.thumb.url %>
              </div>
            <% end %>
            <div class="content">
              <div class="ui centered header"><%= link.title %></div>
            </div>
          <% end %>
        <% end %>
      <% end %>
    </div>
  <% end %>
</div>
<% provide(:title, "Forgot password") %>

<div class="ui blue segment">
	<h3 class="ui header">Forgot your password?</h3>
  <p>Found your password? <%= link_to('Log in', new_user_session_path) %></p>
</div>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, class: "ui large form" }) do |f| %>
  <%= devise_error_messages! %>

  <div class="ui blue segment">

  	<div class="required field <%= "error" if f.object.errors.include?(:email) %>">
  		<%= f.label :email %>
    	<%= f.email_field :email, placeholder: "Email" %>
  	</div>

  </div>

  <%= f.submit "Send me reset password instructions", class: "ui primary button" %>

<% end %>

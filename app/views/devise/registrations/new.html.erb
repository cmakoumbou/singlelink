<% provide(:title, "Sign up") %>

<div class="ui blue segment">
  <h3 class="ui header">Sign up</h3>
  <p>Already have an account? <%= link_to('Log in', new_user_session_path) %></p>
</div>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { class: "ui large form" }) do |f| %>
  <%= devise_error_messages! %>

  <div class="ui blue segment">

    <div class="required field <%= "error" if f.object.errors.include?(:username) %>">
      <%= f.label :username %>
      <%= f.text_field :username, placeholder: "Username" %>
    </div>

    <div class="required field <%= "error" if f.object.errors.include?(:email) %>">
      <%= f.label :email %>
      <%= f.email_field :email, placeholder: "Email" %>
    </div>

    <div class="required field <%= "error" if f.object.errors.include?(:password) %>">
      <%= f.label :password %>
      <%= f.password_field :password, placeholder: "Password" %>
    </div>

    <div class="required field <%= "error" if f.object.errors.include?(:password_confirmation) %>">
      <%= f.label :password_confirmation %>
      <%= f.password_field :password_confirmation, placeholder: "Confirm new password" %>
    </div>
  </div>

  <%= f.submit "Sign up", class: "ui primary button" %>
<% end %>
<% provide(:title, "New link") %>

<%= form_for(@link) do |f| %>
  <%= link_error_messages! %>
  <%= f.text_field :url, placeholder: "Link" %><br />
  <%= f.submit "Create link" %>
<% end %>

<%= link_to('Go back', root_path) %> 